import React from 'react';
import { View } from 'react-native';

const GapList = () => {
  return (
    <View
      style={{
        backgroundColor: 'gray',
        height: 0.5,
        marginStart: 16,
        marginEnd: 16,
        marginTop: 8,
        marginBottom: 8,
      }}
    />
  );
};

export default GapList;
